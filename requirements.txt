# Core automation
ansible-core>=2.15
# Collection dependencies installed separately:
#   ansible-galaxy collection install community.general

# Linting and formatting (also used by pre-commit)
yamllint>=1.35
black>=24.0

# Pre-commit framework
pre-commit>=3.7
