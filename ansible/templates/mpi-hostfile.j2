# MPI hosts file — generated by Ansible from the clients inventory group.
# Add client VMs to hosts.ini and re-run client_setup.yaml to extend.
# Note: multi-client MPI requires VM-to-VM SSH between client nodes.
{% for host in groups['clients'] %}
{{ host }} slots={{ mpi_slots_per_client }}
{% endfor %}
